# The default setup uses the official image directly (see MANIM_IMAGE in .env).
# Build this only when you need extra Python packages in the render sandbox:
#   docker build -t manimate-sandbox sandbox/
#   MANIM_IMAGE=manimate-sandbox
FROM manimcommunity/manim:v0.19.0
# RUN pip install manim-voiceover
